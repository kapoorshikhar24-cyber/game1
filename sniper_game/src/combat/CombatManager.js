export class CombatManager {
    constructor(engine) {
        this.engine = engine;
    }

    // Handles logic for hits, damage, etc.
    handleHit(hitData) {
        // Evaluate armor, zone, penetration here in the future
    }
}
